server:
  port: 3000

spring:
  datasource:
      url: jdbc:h2:mem:examRegister
      driver-class-name: org.h2.Driver
      username: root
      password:
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

info:
  app:
    name: exam-register-service
    version: 0.1.1